#include "iterator.h"

namespace easy_algorithm {


/*Iterator::Iterator(const Iterator& iter) 
  : _pElem(iter.getPointer()) {}*/


/*Elem& Iterator::operator * () const {
  return *_pElem;
}*/


/*Elem* Iterator::operator -> () const {
  return _pElem;
}*/


/*Elem* Iterator::getPointer() const {
  return _pElem;
}*/


/*bool Iterator::operator < (const Iterator& iter) const {
  return *(*this) < *iter ? true : false;
}*/


/*const Iterator operator + (const Iterator& iter, ptrdiff_t diff) {
  return Iterator(iter) += diff;
}


const Iterator operator + (ptrdiff_t diff, const Iterator& iter) {
  return iter + diff;
}


const Iterator operator - (const Iterator& iter, ptrdiff_t diff) {
  return Iterator(iter) -= diff;
}


const Iterator operator ++ (Iterator& iter, int) {
  Iterator ret(iter);
  ++iter;
  return ret;
}


const Iterator operator -- (Iterator& iter, int) {
  Iterator ret(iter);
  --iter;
  return ret;
}*/

}